package locators;

import org.openqa.selenium.By;

public class BalanceEnquiryForm {
	// Locators for the input fields, buttons and select menu on the "Balance Enquiry" page.
		public static By accountNoInputField = By.name("accountno");
		public static By submitButton = By.name("AccSubmit");
	    public static By resetButton = By.name("res");
		
}
